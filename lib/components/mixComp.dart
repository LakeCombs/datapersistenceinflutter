import 'package:flutter/material.dart';

class MixComp extends StatelessWidget {
  const MixComp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
